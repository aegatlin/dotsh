# dotsh

config files for zsh on mac and bash on linux

## install

```sh
git clone git@github.com:aegatlin/dotsh.git ~/.config/sh
# or
gh repo clone dotsh sh

echo "source ~/.config/sh/init.sh" >> .bashrc
# or
echo "source ~/.config/sh/init.sh" >> .zshrc
```
